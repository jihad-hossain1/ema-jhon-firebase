/**
 * 1. create firebase project
 * 2. create web app 
 * 3. npm i firebase
 * 4. save firebase config and export default app
 * 5. build > Authentication > get started > enable sign in method
 * 6. create sing in and sign  
 * 
 * 
 */

/**
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 * 
 * 
 */

/**
 * hosting: 
 * one time per pc
 * 1. npm install -g firebase-tools
 * 1.1 firebase login --interactive
 * 2. firebase login
 * 3. 
 * for each project one time
 * 1. firebase init
 * 2. proced
 * 3. hosting: firebase (up and down arrow) use space bar to select
 * 4. existing project
 * 5. select the project careful
 * 6. which project are you use vite/react main : dist
 * 7. npm run build
 * 8. firebase deploy
 * 
 */